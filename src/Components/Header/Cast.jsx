import React from 'react';
import { NavButton } from './Styles';

const Cast = () => {
    return (
        <NavButton>
            <google-cast-launcher />
        </NavButton>
    );
};

export default Cast;
