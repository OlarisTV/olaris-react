image: node:13.0.1-alpine

variables:

before_script:
    - apk add --no-cache git
    - yarn add npm-run-all

stages:
    - build

compile:
    stage: build
    script:
        - yarn build
    artifacts:
        paths:
            - build/
