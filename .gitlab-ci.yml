image: node:latest

variables:

before_script:
    - yarn install --prod

stages:
    - build

compile:
    stage: build
    script:
        - yarn build
    artifacts:
        paths:
            - build/
