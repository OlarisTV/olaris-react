image: node:10.9.0-alpine

variables:

before_script:
    - yarn add npm-run-all

stages:
    - build

compile:
    stage: build
    script:
        - yarn build
    artifacts:
        paths:
            - build/
